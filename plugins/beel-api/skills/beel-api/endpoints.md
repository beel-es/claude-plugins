# BeeL API — Endpoints Reference

Endpoints change over time — always fetch the live references instead of relying on a static list.

## Live References

- **Full OpenAPI spec**: `https://docs.beel.es/api/openapi`
  Fetch this to get every endpoint, request body, response schema, and example.

- **Documentation index**: `https://docs.beel.es/llms.txt`
  Lists all available doc pages with direct URLs. Fetch individual pages for human-readable descriptions, edge cases, and business rules.

- **Full docs in one request**: `https://docs.beel.es/llms-full.txt`
  Everything in a single file — use when you need broad context.
